/**
 * An enum class that represents the three different orders that a tree can be.
 */
public enum Order {
    /**
     * Preorder order.
     */
    PREORDER,
    /**
     * Inorder order.
     */
    INORDER,
    /**
     * Postorder order.
     */
    POSTORDER;
}
